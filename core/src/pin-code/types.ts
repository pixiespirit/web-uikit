// enum pin code size is linked to the class style in styles/pincode.module.css
export enum SizePinCode {
  MINI = 'inputMini',
  COMPACT = 'inputCompact',
  DEFAULT = 'inputDefault',
  LARGE = 'inputLarge'
}

export enum KeyCodeEnum {
  BACKSPACE = 'Backspace',
  PERIOD = 'Period',
  MINUS = 'Minus',
  DIGIT_INDICATOR = 'Digit'
}
